title: Campus Histories
description: Essays on the history of the University of New Mexico campus
url: ""
baseurl:

# Pages build as directories with an index.html (/directory/ rather than
# /directory.html) so that trailing-slash links work on any static server,
# and so top-level pages match the essay URLs.
permalink: pretty

essay-images: /essays/images
